{
  "domain_name": "hanh_chinh_phap_ly",
  "version": "1.1",
  "fields": {
    
    "loai_van_ban": {
      "type": "keyword",
      "description": "Bắt loại văn bản bằng từ khóa cứng",
      "patterns": [
        "(?i)^(QUYẾT ĐỊNH|CHỈ THỊ|THÔNG BÁO|TỜ TRÌNH|CÔNG VĂN|BIÊN BẢN|BÁO CÁO|KẾ HOẠCH|ĐƠN KHỞI KIỆN|BẢN ÁN)\\b"
      ],
      "validation": {
        "rules": ["not_empty"]
      }
    },

    "co_quan_ban_hanh": {
      "type": "layout_regex",
      "description": "Bắt tên cơ quan dựa trên vị trí (Góc trái, trên cùng) và Regex",
      "layout_hint": {
        "x_max_percent": 0.45, 
        "y_max_percent": 0.20
      },
      "patterns": [
        "(?i)^(ỦY BAN NHÂN DÂN|BỘ|SỞ|TRƯỜNG|CÔNG TY|TÒA ÁN|VIỆN KIỂM SÁT).*$"
      ],
      "validation": {
        "rules": ["not_empty"]
      }
    },

    "so_van_ban": {
      "type": "regex",
      "description": "Số và ký hiệu của văn bản",
      "patterns": [
        "(?i)Số:\\s*([0-9]+(?:\\/[0-9]+)?\\/[A-ZĐa-zđ]+-[A-ZĐa-zđ]+)",
        "(?i)Số:\\s*([0-9]+\\s*\\/\\s*[A-ZĐ]+)",
        "([0-9]+(?:\\/[0-9]+)?\\/[A-ZĐa-zđ]+-[A-ZĐa-zđ]+)"
      ],
      "validation": {
        "rules": ["regex_format"],
        "expected_format": ".*\\/.*" 
      }
    },

    "ngay_thang_nam": {
      "type": "regex",
      "description": "Ngày tháng năm ban hành văn bản",
      "patterns": [
        "(?i)Ngày\\s+([0-9]{1,2})\\s+tháng\\s+([0-9]{1,2})\\s+năm\\s+([0-9]{4})",
        "\\b([0-9]{1,2})\\/([0-9]{1,2})\\/([0-9]{4})\\b"
      ],
      "validation": {
        "rules": ["valid_date"] 
      }
    },

    "cccd": {
      "type": "regex",
      "description": "Số Căn cước công dân nhằm phục vụ khối Tòa án/Bảo hiểm",
      "patterns": [
        "(?i)(?:CCCD|Căn cước công dân|Số định danh).*?([0-9]+)\\b",
        "\\b([0-9]{12})\\b"
      ],
      "validation": {
        "rules": ["exact_length"],
        "target_length": 12
      }
    },

    "llm_batch_extraction": {
      "type": "llm_batch",
      "system_prompt": "Bạn là một chuyên gia bóc tách dữ liệu văn bản pháp lý và hành chính xuất sắc. Hãy trích xuất thông tin theo đúng cấu trúc JSON được yêu cầu. Chỉ lấy dữ liệu có thật, nếu không có hãy trả về null.",
      "fields": {
        "ten_bi_cao": {
          "description": "Họ và tên của bị cáo bị đưa ra xét xử",
          "retrieval_keywords": ["bị cáo", "sinh năm", "trú tại"]
        },
        "toi_danh": {
          "description": "Tội danh mà bị cáo bị truy tố hoặc kết án",
          "retrieval_keywords": ["tội", "khoản", "điều", "bộ luật hình sự"],
          "validation": {"rules": ["not_empty"]}
        }
      }
    }
  }
}