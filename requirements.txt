# ==========================================
# MODULE 1: IMAGE PREPROCESSING
# ==========================================
# Cố định bản 4.8.1.78 để không xung đột với numpy và albumentations
opencv-python-headless==4.8.1.78  
Pillow>=10.2.0
PyYAML>=6.0
matplotlib>=3.8.0

# ==========================================
# MODULE 2: OCR ENGINE (PADDLE + VIETOCR)
# ==========================================
# 1. PaddleOCR (Text Detection)
paddleocr==2.8.1
# BẮT BUỘC giữ Numpy 1.26.4 (Tuyệt đối không lên 2.0 để cứu PaddleOCR)
numpy==1.26.4

# 2. VietOCR (Text Recognition)
vietocr==0.3.13
gdown==4.4.0
# BẮT BUỘC hạ cấp Albumentations xuống 1.3.1 để không đòi OpenCV mới
albumentations==1.3.1

# ==========================================
# CORE AI FRAMEWORK (PYTORCH GPU - CUDA 11.8)
# ==========================================
# Lệnh dưới đây giúp pip tự động biết chỗ tải bản GPU thay vì bản CPU
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.2.2
torchvision==0.17.2